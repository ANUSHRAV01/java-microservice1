spring:
  application:
    name: CONFIG-SERVER
#  cloud:
#    config:
#      server:
#        git:
#          uri:

server:
  port: 9196

eureka:
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      DefaultZone: http://localhost:8761/eureka/
  instance:
    hostname: localhost
